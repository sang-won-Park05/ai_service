# ============================================
# 🔑 API Keys & Credentials (Sample Only)
# 실제 키는 .env 에 넣고 절대 공개하지 마세요
# ============================================

# ---------- OpenAI API ----------
OPENAI_API_KEY=your-openai-api-key

# 사용할 임베딩 모델 (기본: text-embedding-3-large)
EMBEDDING_MODEL=text-embedding-3-large

# 사용할 LLM 모델 (예: gpt-4.1, gpt-4.1-mini 등)
LLM_MODEL=gpt-4.1


# ---------- OpenSearch ----------
OPENSEARCH_HOST=https://your-opensearch-endpoint.ap-northeast-2.aoss.amazonaws.com
OPENSEARCH_REGION=ap-northeast-2
OPENSEARCH_INDEX_NAME=medinote-index
OPENSEARCH_USERNAME=your-username      # serverless는 보통 Access키 사용
OPENSEARCH_PASSWORD=your-password      # serverless는 보통 Access키 사용

# 벡터 차원 (OpenAI text-embedding-3-large = 3072)
EMBEDDING_DIM=3072


# ---------- AWS Credentials (if needed for OpenSearch Serverless) ----------
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key


# ---------- LangSmith (옵션) ----------
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=medinote-rag


# ---------- Multimodal / STT / OCR ----------
# Whisper small (로컬 STT) 사용시: local
# Whisper API 사용 시: gpt-4o-mini-transcribe
STT_MODEL=whisper-small

# OCR 엔진 선택 (paddleocr or gpt-vision)
OCR_ENGINE=paddleocr


# ---------- General Settings ----------
ENV=development
DEBUG=true
